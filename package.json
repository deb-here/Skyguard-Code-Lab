{
  "name": "collab-python-lab",
  "private": true,
  "workspaces": [
    "apps/web",
    "apps/collab-server"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace=apps/web",
    "dev:collab": "npm run dev --workspace=apps/collab-server",
    "build:web": "npm run build --workspace=apps/web"
  }
}
